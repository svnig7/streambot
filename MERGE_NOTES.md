# Merge notes: telestream-bot → streambot

This bot is **streambot (WOODStream)** with **telestream-bot's** distinguishing
features merged in on top. streambot's architecture, plugin system, Mongo
schema, and multi-client byte-streaming engine were kept as the base; nothing
from telestream-bot's own backend code was copied wholesale except its two
frontend HTML pages (see below).

## Why this direction (streambot as base)

Both bots solve the same problem (Telegram file -> shareable stream link) but
telestream-bot is a leaner, TTL/playlist-oriented rewrite while streambot is
the older, more feature-complete bot (persistent per-user file library,
broadcast, ban/unban, force-sub, channel support, multi-client bandwidth
scaling). Per your direction, streambot's DB schema (files tied to a
persistent per-user library) is the source of truth, and telestream-bot's
extra capabilities were ported on top of it rather than the other way round.

## What got merged in, and how

### 1. Playlists (from telestream-bot's `/stream -pl`)
- New `playlist` Mongo collection (`WOODStream/utils/database.py`:
  `add_playlist` / `get_playlist` / `rm_playlist`).
- New `WOODStream/utils/playlist_utils.py`: when you send Telegram an
  **album** (private chat or channel), the bot waits ~1.5s for the rest of
  the album to arrive, reads it back with `get_media_group()`, stores each
  item as a normal file (tagged with a shared playlist token + index), and
  replies with **one** playlist link instead of one reply per file.
- Every file doc that belongs to a playlist carries `pl` (playlist token)
  and `pi` (its index in the playlist).

### 2. TTL / self-expiring links (from telestream-bot's `/stream -ttl`)
- File docs gained an optional `exp` field (epoch seconds).
- `Database.get_file()` now checks `exp` and auto-deletes + raises
  `FIleNotFound` for anything past its expiry, so an expired link behaves
  exactly like a deleted one everywhere in the codebase.
- New reply command **`/ttl 1d2h`** (`WOODStream/bot/plugins/links.py`):
  reply to any of the bot's own link messages to set/clear its expiry.
  Accepts `d`/`h`/`m`/`s`; `/ttl off` clears it.
- A background sweep (`ttl_cleanup_loop` in `__main__.py`, interval set by
  `CLEANUP_INTERVAL`, default 600s) proactively purges expired files and
  playlists so they don't just sit in Mongo.

### 3. Custom thumbnails / cover art (from telestream-bot's `-t`/`-poster`)
- File docs gained an optional `poster` field (an image URL).
- New reply command **`/poster <url>`** (also in `links.py`).
- `GET /poster/{id}` redirects to it; falls back to the parent playlist's
  poster if the file itself has none.
- Both `/ttl` and `/poster` are gated by ownership: only the uploader (or
  `OWNER_ID`) can change them, matching telestream-bot's owner/sudo checks.

### 4. Enhanced player: multi-audio/subtitle track switching
telestream-bot's `web/stream.html` and `web/playlist.html` are used
**verbatim** (they're self-contained, loading `@libmedia/avplayer` from a
CDN for in-browser demuxing/track-switching) - copied into
`WOODStream/template/watch.html` and `WOODStream/template/playlist.html`.
Only the *backend* they talk to was reimplemented, in
`WOODStream/server/stream_routes.py`, against streambot's Mongo schema and
its existing `ByteStreamer` (multi-client, workload-balanced byte-range
reader) instead of telestream-bot's `iter_range`/`probe` helpers:

| Route | Purpose |
|---|---|
| `GET /xstrm/{id}` | the enhanced player page |
| `GET /playlist/{id}` | the playlist viewer page |
| `GET /stream/{id}` | inline (`Content-Disposition: inline`) byte-range stream - what the player's `<video>`/`<audio>` actually points at |
| `GET /api/stream/{id}` | JSON metadata (name/size/mime/playable + playlist prev/next nav) |
| `GET /api/tracks/{id}` | `ffprobe`-derived audio/subtitle track list |
| `GET /subs/{id}/{idx}` | on-the-fly `ffmpeg` WebVTT subtitle extraction |
| `GET /poster/{id}` | cover art redirect |
| `GET /api/playlist/{id}` | JSON listing of a playlist's items |

The existing `/watch/{id}` (Jinja templates `dl.html`/`opleechplay.html`)
and `/dl/{id}` (attachment download) routes are untouched, so old links keep
working exactly as before. New links generated by the bot point video/audio
at `/xstrm/{id}` (the enhanced player) instead.

`ffmpeg`/`ffprobe` are optional: `ENABLE_SUBTITLES`/`ENABLE_TRACK_PROBE`
default to `true` but silently turn themselves off if the binaries aren't on
`PATH` - same behaviour telestream-bot had. The `Dockerfile` now installs
`ffmpeg` (which provides both binaries).

### 5. Bandwidth scaling / bot pool - kept streambot's, not ported
streambot already had a more capable answer to the problem telestream-bot's
`STREAM_BOT_TOKENS` + `DUMP_CHAT_ID` solves: every uploaded file is
re-uploaded once to `FLOG_CHANNEL`, and each `MULTI_TOKEN*` client fetches
its own `file_id` by reading that channel - so extra bot accounts never need
to be members of the *source* chat. This is the same idea as
telestream-bot's dump-chat pattern, just built in from the start and
workload-balanced (`work_loads`) rather than round-robin. Nothing from
telestream-bot's `client_pool.py`/`streaming.py` was needed.

**If you were relying on `STREAM_BOT_TOKENS`/`DUMP_CHAT_ID` env vars from
telestream-bot:** use `MULTI_TOKEN1`, `MULTI_TOKEN2`, ... instead (see the
existing streambot README section on multi-client mode). No `DUMP_CHAT_ID`
is needed - `FLOG_CHANNEL` already serves that purpose.

## New/changed environment variables

All optional, all with defaults - no config changes are required to keep
running an existing streambot deployment as-is.

| Var | Default | Purpose |
|---|---|---|
| `MAX_PLAYLIST_ITEMS` | `50` | cap on files bundled into one playlist from an album |
| `CLEANUP_INTERVAL` | `600` | seconds between TTL sweeps |
| `ENABLE_SUBTITLES` | `true` | subtitle extraction (needs `ffmpeg`) |
| `ENABLE_TRACK_PROBE` | `true` | audio/subtitle track listing (needs `ffprobe`) |

## New commands

- `/ttl <1d2h30m> | off` - reply to a link message to set/clear its expiry.
- `/poster <image-url>` (alias `/thumb`) - reply to a link message to set
  custom cover art.
- Sending an **album** (private chat or channel) now produces one playlist
  link instead of N individual links.

## Migration notes

- No manual DB migration needed. All new fields (`pl`, `pi`, `exp`,
  `poster`) are optional/absent on existing documents; every code path that
  reads them uses `.get(...)` with a falsy default.
- `Database.ensure_indexes()` runs automatically on bot startup
  (`__main__.py`) to add the new indexes; it's a no-op on repeated runs.
- Old `/watch/{id}` and `/dl/{id}` links keep working unchanged.

## Known limitations / things not attempted

- Multi-audio track switching relies entirely on the ported `avplayer` WASM
  frontend from telestream-bot (loaded from a CDN); no server-side
  transcoding/remuxing was added.
- `/ttl` and `/poster` only work as replies to the bot's own link messages
  (private chat), matching how ownership is verified. There's no
  channel-side equivalent yet.
- Playlist TTL is settable via the DB layer (`add_playlist(..., exp=...)`)
  but there's currently no bot command wired up to set a TTL on an entire
  playlist at once (only per-file, and only after the fact via `/ttl`).

## Round 2: caption-preserving link cards + neon premium theme

- **Link message format changed** (`gen_link`/`gen_linkx` in
  `WOODStream/utils/bot_utils.py`): the reply now reproduces the original
  upload's caption verbatim (if it had one - captured via Pyrogram's
  `message.caption.html`, stored as `caption_html` on the file doc), followed
  by a plain `FILE NAME :` / `FILE SIZE :` / `STREAM LINK :` /
  `DOWNLOAD LINK :` / `FILE LINK :` block and `STREAM / DOWNLOAD / GET FILE /
  REVOKE / CLOSE` buttons. No TMDB or other external metadata lookup was
  added - whatever caption the file already had (e.g. forwarded from a
  channel that includes title/plot/etc.) is what shows up. Files with no
  caption just get the plain link block.
- **Neon premium theme** applied to all four web pages:
  `template/dl.html` (rewritten standalone, no more external CSS
  dependency), `template/opleechplay.html`, `template/watch.html`
  (the `/xstrm` enhanced player), and `template/playlist.html`. All four
  already used CSS custom properties for their palette, so this was a
  `:root` variable swap (cyan `#00fff2` / magenta `#ff2bd6` on near-black)
  plus a handful of appended glow rules (`box-shadow`/`text-shadow`) on
  cards, buttons, and the topbar - no structural HTML/JS was touched.
